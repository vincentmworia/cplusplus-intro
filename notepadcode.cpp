#include <iostream>

int main(){
 std::cout << "Hello windows from command line" << std::endl;
 return 0;
}

// To compile and make exe file,  we use: g++ -Wall (to turn on the warnings) -std=c++14 main.cpp -o main.exe (If we want the executable to be named main). 
// Then dir, we find an executable file that we can run now by typing the name of the file.